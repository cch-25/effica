# Contract change request
- Feature: A8 실제 API 연결 및 OpenAPI 타입 생성
- Current contract: `contracts/openapi.json` 기준선이 저장소에 존재하지 않음
- Blocking user flow: 생성 타입·클라이언트 기반 실제 FastAPI 연결, mock/real contract fixture 공유, contract checksum 보고
- Requested change: MAS_B 소유 경로에 정규화된 `contracts/openapi.json`, `contracts/checksum.txt`, `contracts/CHANGELOG.md` 게시
- Backward-compatible alternative: 없음. 승인 전 mock 필드를 실제 API 필수 필드로 추측하지 않음
- UI deadline impact: A0~A7 mock 구현과 UI 검증은 진행 가능하나 A8 실제 연동·contract E2E는 기준선 게시까지 보류
